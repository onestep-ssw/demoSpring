spring:
  thymeleaf:
    prefix:
      classpath: /templates
    cache: false
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: 12345
logging:
  level:
    root: info
config:
  jwt:
    secret: abcdefghijk
    expire: 3600
    header: token
server:
  port: 60001
